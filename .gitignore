# Environment files (NEVER commit these!)
.env
.env.local
.env.production
.env.*

# Python
__pycache__/
*.pyc
*.pyo

# Database
db.sqlite3
db_data/

# Media
media/

# Docker runtime data (db + uploads live here on the host)
data/

# Local backups (server-side only)
backups/

# Collected static (built inside Docker image)
staticfiles/

# Let's Encrypt certs (on the Droplet only)
certbot/

# IDE
.vscode/
.idea/
.claude/
